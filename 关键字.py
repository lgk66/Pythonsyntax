'''True  False  None 空值或者无值
and  or  not
if elif(否则如果 else if)  else
for while break  continue
try except(捕获异常) finally raise(抛出异常)
def(定义函数) return lambda(创建匿名函数)
class del(删除对象引用)
import  from(从模块导入特定部分)  as（为导入的模块或者对象创建别名）
global（声明全局变量）
async await
assert (断言，用于测试条件是否为真)
in(检查成员关系)  is(检查对象身份（是否是同一个对象）)
pass（空语句，用于占位）
with（上下文管理器，用于资源管理）
yield(从生成器函数返回值)
'''